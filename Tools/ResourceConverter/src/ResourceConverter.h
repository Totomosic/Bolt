#pragma once
#include "Debug.h"

#include "Converters/ImageConverter.h"

namespace Bolt
{

	class ResourceConverter
	{
	public:
		ResourceConverter(const std::string& inputFile, const std::string& outputFile);
	};

}